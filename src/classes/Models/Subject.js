
export default class Subject{
    //Groups is a dictionary, wich keys are the group names (GR1, GR2) and the values are the 2D arrays
    //of the hours of classes
    groups = [];
    name;
    constructor(name){
        this.name = name;
    }
}

